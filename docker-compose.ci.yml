name: nanahoshi-ci

services:
  postgres:
    image: groonga/pgroonga:4.0.8-alpine-18
    environment:
      POSTGRES_DB: "${DB_NAME:?DB_NAME is required}"
      POSTGRES_USER: "${DB_USER:?DB_USER is required}"
      POSTGRES_PASSWORD: "${DB_PASSWORD:?DB_PASSWORD is required}"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 2s
      timeout: 5s
      retries: 30
    volumes:
      - postgres-data:/var/lib/postgresql

  redis:
    image: redis:8.10.1-alpine
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:?REDIS_PASSWORD is required}"]
    environment:
      REDIS_PASSWORD: "${REDIS_PASSWORD:?REDIS_PASSWORD is required}"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$${REDIS_PASSWORD}\" ping | grep -q PONG"]
      interval: 2s
      timeout: 3s
      retries: 30

  server:
    stop_grace_period: 40s
    image: nanahoshi:ci
    ports:
      - "127.0.0.1:${CI_API_PORT:?CI_API_PORT is required}:3000"
    environment:
      ENVIRONMENT: production
      APP_URL: http://127.0.0.1:${CI_API_PORT:?CI_API_PORT is required}
      CORS_ORIGIN: http://127.0.0.1:${CI_API_PORT:?CI_API_PORT is required}
      SERVER_URL: http://127.0.0.1:${CI_API_PORT:?CI_API_PORT is required}
      NAMESPACE_UUID: "${NAMESPACE_UUID:?NAMESPACE_UUID is required}"
      DOWNLOAD_SECRET: "${DOWNLOAD_SECRET:?DOWNLOAD_SECRET is required}"
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: "${DB_USER:?DB_USER is required}"
      DB_PASSWORD: "${DB_PASSWORD:?DB_PASSWORD is required}"
      DB_NAME: "${DB_NAME:?DB_NAME is required}"
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: "${REDIS_PASSWORD:?REDIS_PASSWORD is required}"
      BETTER_AUTH_SECRET: "${BETTER_AUTH_SECRET:?BETTER_AUTH_SECRET is required}"
      BETTER_AUTH_URL: http://127.0.0.1:${CI_API_PORT:?CI_API_PORT is required}
    volumes:
      - app-data:/app/apps/server/data
      - ./scripts/fixtures/installation.epub:/books/installation.epub:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres-data:
  app-data:
